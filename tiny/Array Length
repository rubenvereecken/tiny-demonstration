int printarray(int[] a, int l) {
  int i = 0;
  while (i < l) {
    write a[i];
    if (i < l - 1) { // Don't print a comma after the last one
      write ", ";
    }
    i = i + 1;
  }
  writeln "";
}


int tiny() {
  int[] a = [1,2,3,4,5];
  printarray(a, length(a));
}

